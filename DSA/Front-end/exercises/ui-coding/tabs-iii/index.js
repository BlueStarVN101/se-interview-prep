import { createElement } from 'react';

/**
 * @param {{ tabs: Array<{ id: string, label: string, panel: import("react").ReactNode }> }} props
 * @returns {import("react").ReactElement}
 */
export default function TabsIii({ tabs }) {
  throw 'Not implemented';
}
