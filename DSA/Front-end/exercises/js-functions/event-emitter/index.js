/**
 * Event Emitter — see README.md
 */
export class EventEmitter {
  constructor() {
    /** Instance ready; methods throw until implemented. */
  }

  on(event, handler) {
  throw 'Not implemented';
  }

  off(event, handler) {
  throw 'Not implemented';
  }

  emit(event, ...args) {
  throw 'Not implemented';
  }
}
